import styles from "./CurrentSessionControlsModule.module.css";

/**
 * Displays the controls for the active session.
 * @returns {JSX.Element}
 */
const CurrentSessionControlsModule = () => {
    return <div>CurrentSessionControlsModule</div>;
};

export default CurrentSessionControlsModule;
