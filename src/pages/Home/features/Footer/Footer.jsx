import styles from "./Footer.module.css";

/**
 * The site footer to be shown on the Home page.
 * @return {JSX.Element}
 */
const Footer = () => {
    return (
		<footer className={styles.footer}>
			<div>
				
			</div>
		</footer>
	);
};

export default Footer;
